# .env-template for Web Crawler MCP Server

# Server configuration
PORT=3000                        # MCP server listening port
NODE_ENV=development             # Environment mode (development|production)

# MCP metadata
MCP_NAME=WebCrawler-MCP             # MCP server name
MCP_VERSION=1.0.0                 # MCP server version
MCP_DESCRIPTION=MCP Server for web crawling  # MCP server description

# Default crawl settings
CRAWL_DEFAULT_MAX_PAGES=10        # Default maximum pages to crawl
CRAWL_DEFAULT_DEPTH=3             # Default crawl depth
CRAWL_DEFAULT_STRATEGY=bfs        # Default crawl strategy (bfs|dfs|bestFirst)
CRAWL_DEFAULT_WAIT_TIME=1000      # Default wait time between requests (ms)

# Logging
LOG_LEVEL=info                    # Logging level (debug|info|warn|error)

# Cache settings
CACHE_TTL=3600                    # Cache TTL in seconds

# Request handling
MAX_REQUEST_SIZE=10mb             # Maximum JSON request body size

# CORS configuration
CORS_ORIGINS=*                    # Comma-separated list of allowed CORS origins

# Rate limiting
RATE_LIMIT_WINDOW=900000          # Rate limit window in milliseconds (15 minutes)
RATE_LIMIT_MAX_REQUESTS=100       # Max requests per RATE_LIMIT_WINDOW

# Puppeteer configuration
PUPPETEER_EXECUTABLE_PATH=        # Custom path to Chrome/Chromium executable (optional)
PUPPETEER_SKIP_DOWNLOAD=false     # Skip automatic Chromium download during installation
